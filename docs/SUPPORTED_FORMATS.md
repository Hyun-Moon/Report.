# 지원 가능한 양식 / 지원 불가능한 양식

새 양식을 만들기 전에 이 문서를 먼저 보면, "이 템플릿이 될까?" 를 미리 판단할 수
있습니다. 아래 표의 ✅ 항목은 모두 `tests/test_matrix.py` 로 **실제 파일을 만들어
돌려보고 결과를 다시 열어 확인한** 것들입니다.

---

## 1. 워드 템플릿 (.docx)

### 되는 것

| 구조 | 상태 | 검증 케이스 |
|---|---|---|
| 표 없이 본문 문장 사이에 `{{태그}}` | ✅ | W1 |
| 병합 없는 일반 표 | ✅ | W2 |
| 가로·세로 병합이 섞인 복잡한 표 (지출결의서형) | ✅ | W3 |
| 표가 여러 개 섞인 문서 (회의록형) | ✅ | W4 |
| 표 안의 표 (중첩 표) | ✅ | W3/W4 스캔 |
| 같은 태그가 문서 안에서 여러 번 반복 | ✅ | W5 |
| 머리글 / 바닥글의 태그 | ✅ | W6 |
| 첫 페이지·짝수 페이지 전용 머리글/바닥글 | ✅ | 스캔 지원 |
| 행 수가 유동적인 표 (`{%tr for %}` 반복) | ✅ | W7 |
| 한글·공백·괄호·하이픈이 든 태그 이름 (`{{연-월}}`) | ✅ | W8 |
| 텍스트 상자·도형 안의 태그 | ✅ | W9 |
| 첫 열이 세로로 병합되어 항목을 묶는 표 (그룹 라벨) | ✅ | W10 |
| 글머리 기호·번호 매기기 목록 안의 태그 | ✅ | W11 |
| 태그 글자가 여러 서식 조각(run)으로 쪼개진 경우 (일부만 볼드 등) | ✅ | W12 |
| 워드가 태그를 여러 서식 조각으로 쪼개 놓은 경우 | ✅ | docxtpl `patch_xml` |
| 그림·차트·SmartArt 가 들어 있는 문서 | ✅ | 손대지 않고 그대로 보존 |

### 안 되는 것 / 주의할 것

| 구조 | 이유와 대처 |
|---|---|
| **구형 `.doc`** | 형식 자체가 다릅니다. 워드에서 `.docx` 로 다시 저장하세요. |
| **한글(HWP) 문서** | 지원하지 않습니다. `.docx` 로 변환 후 사용하세요. |
| **암호가 걸린 문서** | 열 수 없습니다. 암호를 푼 사본을 쓰세요. |
| **태그를 반쯤 지운 경우** (`{{사용량}`) | 짝이 안 맞으면 태그로 인식하지 않고 글자 그대로 남습니다. |
| **`{%tr for %}` 와 `{%tr endfor %}` 가 같은 행** | 표식이 든 행은 통째로 사라지므로 반복이 깨집니다. 각각 자기 행을 차지해야 합니다. |
| **`{{ 함수(인자) }}` 형태** | jinja2 식으로 취급되어 매핑 항목에 나오지 않습니다. 태그 이름에 `이름(` 형태를 붙이지 마세요. `{{사용량 (2026-01)}}` 처럼 **괄호 앞에 공백**을 두면 태그로 인식됩니다. |
| **`{{ r.항목 }}`, `{{ 값\|round(1) }}`** | 의도적으로 그대로 둡니다. 반복문 안에서 직접 쓰는 고급 문법용입니다. |
| **각주·미주 안의 태그** | 렌더링은 되지만 스캔 목록에는 나오지 않을 수 있습니다. |
| **자동 목차(TOC)** | 값은 채워지지만 목차 쪽수는 워드에서 `F9` 로 갱신해야 합니다. |

---

## 2. 엑셀 템플릿 (.xlsx / .xlsm)

### 되는 것

| 구조 | 상태 | 검증 케이스 |
|---|---|---|
| 단일 시트, 위아래로 연속된 라벨/값 | ✅ | X1 |
| 여러 시트에 나눠 값 채우기 | ✅ | X2 |
| 열·행을 건너뛰는 불규칙한 배치 | ✅ | X3 |
| 테두리·배경색·글꼴·가운데정렬 유지 | ✅ | X4 |
| 표시형식(`#,##0.0`) 유지 — 숫자가 숫자로 들어감 | ✅ | X4 |
| 셀 병합 유지 | ✅ | X4 |
| **수식 보존** (`=SUM(B2:B3)`, `=IF(...)`) | ✅ | X5 |
| 값을 채운 셀을 참조하는 수식이 정상 계산 | ✅ | X5 (열 때 자동 재계산) |
| `{{#표}}` 자리에 집계표 통째로 삽입 | ✅ | X6 |
| 태그 없이 셀 좌표(`요약!B3`)로 직접 지정 | ✅ | X7 |
| 셀 안에 글자와 태그가 섞인 경우 (`작성자: {{담당자}}`) | ✅ | X2 |
| 시트 이름에 공백이 있는 경우 (`'월간 요약'!B3`) | ✅ | 따옴표 표기 지원 |
| 틀 고정, 조건부 서식, 데이터 유효성 | ✅ | X8 |
| 결재란처럼 작은 셀이 촘촘히 병합된 표 | ✅ | X9 |
| `.xlsm` 매크로 | ✅ | `keep_vba` 로 보존 |

### 안 되는 것 / 주의할 것

| 구조 | 이유와 대처 |
|---|---|
| **구형 `.xls`** | 엑셀에서 `.xlsx` 로 다시 저장하세요. |
| **차트·이미지·도형** | openpyxl 이 파일을 다시 쓰면서 **소실될 수 있습니다.** 로고나 차트가 들어간 양식은 생성 후 결과 파일을 반드시 눈으로 확인하세요. 소실된다면 차트는 결과 파일에 나중에 붙이거나, 워드 템플릿 쪽을 쓰는 편이 안전합니다. |
| **피벗 테이블** | 정의는 남지만 새로 고침이 필요할 수 있습니다. |
| **수식의 '계산된 값'** | openpyxl 은 계산 결과 캐시를 보존하지 못합니다. 그래서 파일을 열 때 자동 재계산되도록 표시해 둡니다. 엑셀 없이 결과 파일을 프로그램으로 읽으면 수식 셀이 비어 보일 수 있습니다. |
| **머리글/바닥글(페이지 설정)의 태그** | 셀이 아니라 인쇄 설정이라 치환되지 않습니다. |
| **도형·텍스트 상자 안의 태그** | 워드와 달리 엑셀에서는 치환되지 않습니다. 셀에 적어 주세요. |
| **병합된 셀의 좌상단이 아닌 칸에 값 지정** | 엑셀 구조상 불가능합니다. 자동으로 좌상단 칸에 씁니다. |
| **`{{#표}}` 아래에 기존 내용이 있는 경우** | 표가 그 위에 덮어씁니다. 앵커 아래에는 여유를 두세요. |

---

## 3. 원본 데이터 엑셀

### 되는 것

| 구조 | 상태 | 검증 케이스 |
|---|---|---|
| 헤더 1행 | ✅ | S1 |
| 헤더 2행 (상위 헤더 병합) → `상위 / 하위` 로 합침 | ✅ | S2 |
| 표가 시트 왼쪽 위에서 시작하지 않음 (제목 줄이 위에 있음) | ✅ | S3 |
| 빈 셀·빈 행이 섞임 | ✅ | S3 |
| 숫자·문자·날짜·`1,234` 형태 문자열 혼재 | ✅ | S4 |
| 데이터 행이 200행 이상 | ✅ | S11 |
| 데이터 행이 1행뿐 | ✅ | S12 |
| 셀 범위 직접 지정 (`B3:G40`) | ✅ | |
| 날짜가 열 방향으로 늘어선 표 → 전치 옵션 | ✅ | S10 / A7 |
| 컬럼 이름이 중복 → `이름`, `이름_1` 로 구분 | ✅ | |
| 제목 블록이 위 여러 행 + 표 자체도 왼쪽 여러 열이 빈 경우 | ✅ | S16 |
| 데이터 본문 안에 병합 셀 (부서명이 날짜 여러 행에 걸쳐 병합) | ✅ | S17 |
| 요약/합계 행이 날짜 데이터 사이·아래에 섞여 있음 | ✅ | S20 (날짜 아님 자동 스킵) |
| `1,250kWh` 처럼 숫자에 단위가 붙은 문자열 | ✅ | S15 |
| 음수 값 | ✅ | S15 |
| **한 시트에 표가 여러 개 뒤섞임** (좌우로 나란히 + 위아래로 별개) → **[표 후보 찾기…]** 로 원하는 표를 골라 범위 자동 채움 | ✅ | S21 (대형빌딩 에너지 대장처럼 전력/스팀/용수가 나란히, 그 아래 또 다른 표가 이어지는 구조에서 5개 후보를 정확히 분리) |
| **계산 결과 캐시가 없는 수식 셀**(원본을 만든 프로그램이 계산해 두지 않은 경우)도 **`formulas` 라이브러리로 워크북 전체를 계산**해서 채웁니다 (엑셀을 다시 열 필요 없음) — 셀 참조 덧셈·뺄셈, `SUM`/`AVERAGE`/`IF` 같은 기본 함수는 물론 **다른 시트 참조, `VLOOKUP`/`INDEX`/`MATCH`/`XLOOKUP`** 등 실무에서 흔한 대부분의 엑셀 함수까지 계산됩니다 | ✅ | S18(본문 `=B2+C2`), S22(하루씩 더해가는 날짜 헤더 `=B3+1` 연쇄), S23(다른 시트 참조 `=요약!A1`) |

### 안 되는 것 / 주의할 것

| 구조 | 이유와 대처 |
|---|---|
| **헤더가 3행 이상** | 자동 감지는 2행까지만 추정합니다. 1단계에서 헤더 행 수를 직접 입력하세요. |
| **세로로 라벨-값이 반복되는 카드형 배치** | 표 형태가 아니면 읽지 못합니다. 전치 옵션으로 해결되는 경우가 있습니다. |
| **`.csv`** | 현재는 `.xlsx` / `.xlsm` 만 지원합니다. 엑셀에서 열어 저장하세요. |
| **계산 캐시가 없는데, `formulas` 라이브러리도 못 푸는 수식** (존재하지 않는 외부 파일 참조, 순환 참조, 극히 일부의 배열 수식 등) | 대부분의 함수는 계산되지만, 100%는 아닙니다 — 잘못 계산해서 '그럴듯하지만 틀린 값'을 내놓는 것이, 캐시가 없다고 솔직하게 말하는 것보다 훨씬 위험하므로, 계산이 안 되는 경우는 안전하게 걸러냅니다. 기본은 **`FormulaCacheError`로 바로 알려줍니다** (어느 셀이 문제인지 좌표까지 표시) — 엑셀(또는 LibreOffice Calc)에서 한 번 열고 저장한 뒤 다시 시도하세요. 재계산이 불가능한 상황이라면, 1단계의 **"계산되지 않은 수식 셀은 빈 값으로 넘어가기"** 체크박스를 켜서 그 칸만 비운 채 진행할 수도 있습니다(해당 값은 집계에서 빠짐). **헤더 행 자체**가 이런 수식이어도 똑같이 걸러집니다 — 그러지 않으면 `열C`, `열D` 같은 엉뚱한 컬럼 이름이 조용히 생깁니다. 검증: S24(없는 외부 파일 참조). |
| **`formulas` 라이브러리의 설치 용량** | scipy·numpy 를 딸려 설치하므로 기존 3개 패키지보다 훨씬 큽니다(수십~백 MB). 사내 PC 인터넷이 느리거나 막혀 있으면 설치가 오래 걸리거나 실패할 수 있습니다. |
| **표 후보 찾기가 논리적으로 하나인 표를 둘로 쪼갬** | 표 중간에 완전히 빈 행/열이 있으면(디자인상의 여백 등) 별개 표로 인식할 수 있습니다. 그럴 땐 후보 목록에서 나온 범위를 참고해 셀 범위 칸에서 직접 합쳐 입력하세요. |
| **표 여러 개를 한 번에 하나의 보고서로 자동 결합** | [표 후보 찾기]는 "어디부터 어디까지가 한 표인지"만 찾아줍니다. 서로 다른 표(예: 전력·스팀·용수)를 하나의 보고서에 합치려면, 표마다 따로 읽어 결과를 받거나 원본 엑셀에서 미리 한 시트로 정리해 두는 편이 낫습니다. |

---

## 4. 일 → 월 집계

### 되는 것

| 상황 | 상태 | 검증 케이스 |
|---|---|---|
| 월 중간 날짜 누락 (28일치만 있음) | ✅ | W2 (28일 집계) |
| 여러 달 혼재 → 월별 보고서 각각 | ✅ | A1 (3개 파일) |
| 여러 달 혼재 → 한 보고서에 월별 나열 | ✅ | W7 / W8 / X6 |
| `2026-01-05` / `2026/01/05` / `2026.01.05` | ✅ | A2 |
| `20260105` / `2026년 1월 5일` | ✅ | A2 |
| 엑셀 일련번호 (`46059`) | ✅ | A2 |
| `01/05` (연도 없음) → 기준 연도로 보완 | ✅ | A2 |
| `5일` (연·월 없음) → 기준 연/월 입력 필요 | ✅ | A3 |
| `2026-01-05 09:30` (시간 포함) | ✅ | S4 |
| `2026-07-01(수)` (날짜 뒤에 요일이 괄호로 붙음) | ✅ | A8 |
| 같은 날짜에 여러 번 기록 (오전/오후 검침 등) → 그 날짜로 합쳐 집계 | ✅ | A9 |
| 주말 제외 | ✅ | A4 |
| 주말 + 공휴일 제외 | ✅ | A5 |
| 특정 월만 골라서 집계 | ✅ | A6 |
| 항목별로 다른 집계 방식 (합산/평균/최대/최소/건수/첫값/마지막값) | ✅ | 전 케이스 |
| `1,234` 같은 문자열 숫자 합산 | ✅ | S4 |
| `1,250kWh` 처럼 단위가 붙은 문자열, 음수 값 | ✅ | A10 |
| 결측 없이 꽉 찬 한 달 (정상 케이스 대조) | ✅ | A11 |
| 요약/합계·작성자 서명 같은 비(非)날짜 행이 섞여도 자동 스킵 | ✅ | A12 |
| **가동시간이 엑셀 시간형식(`[h]:mm` 누적시간, `h:mm` 시각, `'8:30'` 문자열)** | ✅ | C3 (전부 `8.5시간` 으로 정확히 환산되어 합산) |
| 값이 전부 0인 달(설비 미가동 등) → 빠진 값이 아니라 진짜 0으로 집계 | ✅ | C4 |
| `1호기`, `3동 2층` 처럼 숫자가 섞인 이름표 컬럼은 자동으로 '합산' 추천에서 제외 | ✅ | C5 |
| 미리보기에서 값을 손으로 고쳐 반영 | ✅ | GUI 4단계 |

### 안 되는 것 / 주의할 것

| 상황 | 이유와 대처 |
|---|---|
| **숫자 컬럼이 날짜로 잘못 잡힘** | 컬럼 이름에 날짜 힌트(`일자`, `날짜`, `date` 등)가 없는 맨숫자 컬럼은 날짜 후보에서 제외합니다. 그래도 헷갈리면 4단계에서 날짜 컬럼을 직접 고르세요. |
| **`5일` 처럼 일자만 있는 표에서 기준 연/월 미입력** | 어느 달인지 알 수 없어 집계가 실패합니다. 기준 연/월을 반드시 입력하세요. |
| **주 단위·분기 단위 집계** | 현재는 월 단위만 지원합니다. |
| **공휴일 자동 인식** | 인터넷을 쓰지 않으므로 공휴일은 직접 입력해야 합니다. |
| **여러 달 나열 모드에서 달을 지정하지 않은 태그** | `{{사용량}}` 처럼 달이 없는 태그는 어느 달인지 알 수 없어 `3596 / 6006 / 3055` 처럼 전부 나열됩니다. 특정 달을 원하면 `{{사용량 (2026-01)}}` 로 쓰세요. |
| **1900-02-29 이전 날짜의 엑셀 일련번호** | 엑셀 자체의 윤년 버그 구간이라 하루 어긋날 수 있습니다. |
| **`%` 가 붙은 값을 집계하면 값이 100분의 1로 바뀜** | `-3.5%` 는 내부적으로 `-0.035` 로 계산됩니다(엑셀이 퍼센트 서식 숫자를 저장하는 방식과 동일). 평균 낸 결과를 그대로 보고서에 쓰면 `-0.0246` 처럼 나오니, 템플릿에서 숫자 형식이나 `×100` 표기가 필요하면 매핑 화면의 '숫자 형식'/'단위' 칸으로 보정하세요. |

---

## 5. 오류가 났을 때

프로그램은 스택 트레이스 대신 사람이 읽는 메시지와 해결 방법을 보여 줍니다.

| 메시지 | 해결 |
|---|---|
| `'.txt' 형식은 읽을 수 없습니다` | `.xlsx` / `.xlsm` 파일을 고르세요. |
| `'○○' 시트가 없습니다` | 메시지에 나온 시트 목록에서 고르세요. |
| `셀 범위 'A1-Z9' 를 이해할 수 없습니다` | `B3:G40` 형식으로 입력하세요. |
| `날짜 컬럼을 찾지 못했습니다` | 4단계에서 날짜 컬럼을 직접 지정하거나 기준 연/월을 입력하세요. |
| `'○○' 에 연결된 컬럼이 원본에 없습니다` | 원본을 바꿨다면 3단계에서 매핑을 다시 지정하세요. |
| `결과 파일을 저장하지 못했습니다` | 같은 이름의 파일이 워드/엑셀에 열려 있으면 닫으세요. |
| `집계할 수 있는 날짜 데이터가 없습니다` | 주말/공휴일/대상월 제외 조건이 너무 강하지 않은지 확인하세요. |
| `원본 엑셀에 계산되지 않은 수식 셀이 있어 값을 읽을 수 없습니다` | 내장 계산기가 직접 풀지 못하는 수식(다른 시트 참조, `VLOOKUP` 등 복잡한 함수)일 때만 나옵니다. 엑셀(또는 LibreOffice Calc)에서 파일을 한 번 열고 저장한 뒤 다시 시도하거나, 1단계의 "빈 값으로 넘어가기" 체크박스를 켜세요. 메시지에 문제 셀 좌표가 함께 나옵니다. |

---

## 6. 에너지 관리 업무 문서

전력·가스·유류 사용량 보고, 절감량 산정, 계약전력 관리, 온실가스 배출량처럼
에너지 관리 실무에서 흔한 문서 구조를 `tests/test_energy_matrix.py` 로 별도
검증했습니다.

### 되는 것

| 구조 | 상태 | 검증 케이스 |
|---|---|---|
| 전력(kWh)·가스(Nm³)·유류(L) 등 단위가 다른 여러 에너지원을 한 표에 함께 집계 | ✅ | E1 |
| 15분 간격 AMI(원격검침) 데이터처럼 하루 수십~수백 행이 쌓인 원본 → 월간 합산/최댓값 | ✅ | E2 (384행 → 4일 정상 집계, 시각별 피크 정확히 검출) |
| 기준(baseline) 대비 실적 사용량을 비교해 절감량·절감률을 **엑셀 수식**으로 계산 | ✅ | E3 |
| 계약전력 대비 최대수요 초과 여부를 **IF 수식**으로 판정 | ✅ | E4 |
| 사용량 × 배출계수로 온실가스 배출량을 **엑셀 수식**으로 계산 | ✅ | E5 |
| 에너지진단 개선안처럼 항목 수가 유동적인 목록형 표(투자비·절감액·회수기간) | ✅ | E6 |
| **대형빌딩 냉동기(냉동설비) 여러 대를 설비별 컬럼으로 나눠 집계 + 합계 수식** | ✅ | C1 (3대분 가동시간·전력사용량 각각 정확히 집계, `=SUM(...)` 합계 보존) |
| 가동시간이 엑셀 시간형식(`[h]:mm` 누적시간, `h:mm` 시각, `'8:30'` 문자열)으로 뒤섞여 입력됨 | ✅ | C3 (네 가지 표기 전부 8.5시간으로 정확히 환산되어 합산됨) |
| 냉방기간 외 설비 미가동(가동시간·사용량이 전부 0)인 달 | ✅ | C4 (0을 빠진 값이 아니라 진짜 0으로 정확히 합산) |
| 설비 이름표에 숫자가 섞임(`1호기`, `3동 2층`) → 실수로 합산되지 않음 | ✅ | C5 |

### 안 되는 것 / 주의할 것

| 상황 | 이유와 대처 |
|---|---|
| **원단위(사용량÷생산량), 배출량(사용량×배출계수) 같은 파생 지표를 자동 계산** | 이 프로그램은 원본 컬럼을 그대로 옮기거나(워드/엑셀 공통) 엑셀 템플릿의 수식으로 계산할 뿐, **원본에 없는 값을 스스로 연산하지는 않습니다.** 세 가지 방법 중 하나를 쓰세요. ① 원본 엑셀에 미리 계산해 컬럼으로 넣어 둔다(E7 방식 — 워드/엑셀 템플릿 모두 가능). ② 엑셀 템플릿이면 해당 셀에 수식을 직접 적어 둔다(E3~E5 방식 — 사용량 등 원본값만 채워 넣고 계산은 엑셀이 함). ③ 워드 템플릿인데 원본에 값이 없다면, 매핑 단계에서 "원본에 없는 컬럼입니다" 라는 명확한 오류가 뜨니 그때 원본을 보완하세요. |
| **15분/1시간 단위 데이터에서 '몇 시에 피크가 났는지' 같은 시각 정보** | 최댓값 자체는 정확히 집계되지만(E2), 그 값이 발생한 시각은 보고서에 별도로 나오지 않습니다. 시각까지 필요하면 원본에서 해당 행을 직접 확인하세요. |
| **공식 배출계수·표준 원단위 값의 자동 조회** | 인터넷을 쓰지 않으므로 배출계수 등은 항상 직접 입력(고정값 매핑)해야 합니다. |
| **냉동기 1호기·2호기·3호기 데이터가 한 파일에 세로로(행으로) 섞여 있고, 설비별로 나눈 보고서가 필요한 경우** | 이 프로그램은 '연-월'로만 그룹화하고, 설비명 같은 다른 기준으로는 나누지 않습니다. 세로형식 원본을 그대로 집계하면 **모든 설비의 값이 합쳐진 전체 합계**가 나옵니다(C2로 확인 — 오류 없이 합쳐지므로 알아채기 어렵습니다). 설비별 보고서가 필요하면 ① 원본을 설비별로 시트나 파일을 나누거나, ② 애초에 설비별 컬럼으로 나뉜 넓은(wide) 형식(C1 방식)으로 원본을 구성하세요. |

---

## 7. 새 양식을 만들 때 권장 사항

1. 태그 이름은 **원본 엑셀의 컬럼 이름과 똑같이** 쓰면 자동 매칭이 붙습니다.
2. 엑셀 양식이라면 로고·차트는 되도록 넣지 말고, 필요하면 결과 파일에 나중에
   붙이세요.
3. 처음 쓰는 양식은 **작은 원본 데이터로 한 번 돌려보고 결과를 열어 확인**한 뒤
   실제 업무에 쓰세요.
4. 매핑을 저장해 두면 다음 달부터는 파일만 바꿔 끼우면 됩니다.
